# ============================================
# Docker Compose Environment Variables
# ============================================
# Copy this file to .env and customize as needed

# -----------------
# Test Profile
# -----------------
# Use profiles from config/hono.yaml
PROFILE=smoke              # Options: smoke, standard, stress

# -----------------
# Optional Overrides
# -----------------
# These override the profile settings if specified
TENANTS=                   # Leave empty to use profile default
DEVICES=                   # Leave empty to use profile default
PROTOCOLS=                 # e.g., "mqtt" or "mqtt,http"
MESSAGE_INTERVAL=          # Seconds between messages
DURATION=                  # Test duration in seconds

# -----------------
# Caching
# -----------------
USE_CACHE=true             # Enable device/tenant caching
CLEAR_CACHE=false          # Clear cache before running

# -----------------
# Examples
# -----------------
# Quick smoke test (default):
#   PROFILE=smoke
#
# Custom test with overrides:
#   PROFILE=standard
#   DEVICES=50
#   PROTOCOLS=mqtt,http
#
# HTTP-only test:
#   PROFILE=smoke
#   PROTOCOLS=http
#
# Disable caching:
#   USE_CACHE=false
